{% extends 'base.html.twig' %}

{% block title %}{{ 'edit'|trans }} {{ 'homepage.trainers'|trans }}{% endblock %}

{% block body %}

    <div class="container">
        <div class="row mt-5 mb-3">
            <div class="col">
                <h1>{{ 'edit'|trans }} {{ 'homepage.trainers'|trans }}</h1>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item">
                            <a href="{{ path('app_home') }}">{{ 'index.home'|trans }}</a>
                        </li>
                        <li class="breadcrumb-item">
                            <a href="{{ path('app_trainer_index') }}">{{ 'homepage.trainers'|trans }}</a>
                        </li>
                        <li class="breadcrumb-item active">{{ 'edit'|trans }} {{ 'homepage.trainers'|trans }}</li>
                    </ol>
                </nav>
            </div>
        </div>

        <div class="card shadow">
            <div class="card-body">
                {{ include('trainer/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>

        <div class="d-flex justify-content-between align-items-center mt-5">
            <a href="{{ path('app_trainer_index') }}" class="btn btn-outline-primary"><i class="bi bi-arrow-left"></i> {{ 'edit'|trans }} {{ 'cancel'|trans }}</a>
            {{ include('trainer/_delete_form.html.twig') }}
        </div>

    </div>
{% endblock %}
