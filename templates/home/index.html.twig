{% extends 'base.html.twig' %}

{% block title %}CRUDS{% endblock %}

{% block body %}
    <div class="container vh-100">
        <div class="row justify-content-center align-items-center h-100">
            <div class="col-md-4 mb-3">
                <div class="card shadow text-center">
                    <div class="card-body">
                        <h5 class="card-title mb-5">Employees</h5>
                        <a href="{{ path('app_employee_index') }}" class="btn btn-primary">Go Employees</a>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-3">
                <div class="card shadow text-center">
                    <div class="card-body">
                        <h5 class="card-title mb-5">Courses</h5>
                        <a href="{{ path('app_course_index') }}" class="btn btn-primary">Go Courses</a>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-3">
                <div class="card shadow text-center">
                    <div class="card-body">
                        <h5 class="card-title mb-5">Trainers</h5>
                        <a href="{{ path('app_trainer_index') }}" class="btn btn-primary">Go Trainers</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}