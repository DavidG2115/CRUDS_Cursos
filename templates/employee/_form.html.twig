{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="col d-flex ms-auto flex-row-reverse">
        <button class="btn btn-success mt-4">{{ 'update'|trans }}</button>
    </div>
{{ form_end(form) }}
