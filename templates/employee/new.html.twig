{% extends 'base.html.twig' %}

{% block title %}New Employee{% endblock %}

{% block body %}

    <div class="container">
        <div class="row mt-5 mb-3">
            <div class="col">
                <h1>Create new Employees</h1>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item">
                            <a href="{{ path('app_employee_index') }}">Home</a>
                        </li>
                        <li class="breadcrumb-item">
                            <a href="{{ path('app_employee_index') }}">Home eployees</a>
                        </li>
                        <li class="breadcrumb-item active">New employee</li>
                    </ol>
                </nav>
            </div>
        </div>

        <div class="card shadow">
            <div class="card-body ">
                {{ include('employee/_form.html.twig') }}

                <div class="row mt-5">
                    <div class="col align-items-center">
                        <a href="{{ path('app_employee_index') }}" class="btn btn-outline-primary"><i class="bi bi-arrow-left"></i> Cancel</a>
                    </div>
                </div>
            </div>

        </div>


    </div>
{% endblock %}
