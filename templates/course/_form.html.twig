{{ form_start(form) }}
{{ form_widget(form) }}
    <div class="col d-flex ms-auto flex-row-reverse">
        <button class="btn btn-success m-3">{{ 'update'|trans }}</button>
    </div>
{{ form_end(form) }}

